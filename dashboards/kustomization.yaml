namespace: open-cluster-management-observability

generatorOptions:
 disableNameSuffixHash: true
 labels:
   grafana-custom-dashboard: "true"


configMapGenerator:
- name: gatekeeper-dashboard
  files:
    - gatekeeper.json=grafana.json
- name: rightsizing-dashboard
  files:
    - rightsizing.json=rightsizing.json    
